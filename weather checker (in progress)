#include <iostream>
using namespace std;


int TDF, WMPH, DP;

void input ( double & TDF, double & WMPH, double & DP);

int main()
{
	double TDF;

	cout << "what is the temperature in degrees Fahrenheit" << endl;

	while (!(cin >> TDF)) {  // Keep asking until the user enters a valid number
		cout << "what is the temperature in degrees Fahrenheit" << endl;
		cin.clear(); // Reset input errors
		cin.ignore(10000, '\n'); // Remove bad input
	}

	cout << "what is the windspeed in mph" << endl;

	while (!(cin >> WMPH)) {  // Keep asking until the user enters a valid number
		cout << "what is the windspeed in mph" << endl;
		cin.clear(); // Reset input errors
		cin.ignore(10000, '\n'); // Remove bad input
	}

	cout << "what is the dew point" << endl;

	while (!(cin >> DP)) {  // Keep asking until the user enters a valid number
		cout << "what is the dew point" << endl;
		cin.clear(); // Reset input errors
		cin.ignore(10000, '\n'); // Remove bad input
	}

}
